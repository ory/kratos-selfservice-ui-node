apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-api.fullname" . }}
spec:
  selector:
    app: {{ include "admin-api.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
