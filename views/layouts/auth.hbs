<html lang='en'>
  <head>
    {{> standard_headers}}
    <link type="text/css" rel='stylesheet' href="{{#if basePath }}/{{basePath}}{{/if}}/auth-layout.css" />
  </head>
  <body>
    <main data-testid="app-express">
      <div class="auth-container spacing-16">
        {{{body}}}
        {{> ory_branding}}
      </div>
      
    </main>
  </body>
</html>