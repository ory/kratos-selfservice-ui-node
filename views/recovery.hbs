<div class="content">
    {{> header}}
    <div class="container">
        <h4>Recover your account</h4>

        {{> messages messages=messages className="global"}}

        {{#if token}}
            <div id="recovery-token">
                {{> form form=token submitLabel="Send recovery link"}}
            </div>
        {{/if}}
    </div>
</div>
